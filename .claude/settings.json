{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path // .tool_response.filePath' | { read -r f; case \"$f\" in *.js) node --check \"$f\" && echo \"[build check] syntax OK: $f\" || echo \"[build check] SYNTAX ERROR: $f\";; esac; } 2>/dev/null || true",
            "statusMessage": "Running node --check after edit..."
          }
        ]
      }
    ]
  }
}
